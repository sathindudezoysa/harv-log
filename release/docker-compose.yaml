services:
  loki:
    image: ghcr.io/sathindudezoysa/harv-log-loki:latest
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:3100:3100"
    volumes:
      - loki-data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:3100/ready | grep -q Ready"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  promtail:
    image: ghcr.io/sathindudezoysa/harv-log-promtail:latest
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:9080:9080"
    volumes:
      - ./bundle-logs:/var/log/bundle/bundle-logs:ro
      - promtail-positions:/tmp
    command: -config.file=/etc/promtail/config.yaml
    depends_on:
      loki:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:9080/ready | grep -q ready"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s

  grafana:
    image: ghcr.io/sathindudezoysa/harv-log-grafana:latest
    ports:
      - "${BIND_ADDRESS:-127.0.0.1}:3000:3000"
    environment:
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: harv-logs
      GF_INSTALL_PLUGINS: grafana-llm-app
    depends_on:
      loki:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - grafana-data:/var/lib/grafana
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://localhost:3000/api/health | grep -q 'ok'"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s

volumes:
  loki-data:
  promtail-positions:
  grafana-data:
