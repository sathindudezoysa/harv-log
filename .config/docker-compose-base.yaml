services:
  grafana:
    user: root
    container_name: 'harv-logs'
    image: sathindu/harv-logs:latest

    build:
      context: .
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana}
        grafana_version: ${GRAFANA_VERSION:-13.1.0}
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
      - 2345:2345/tcp # delve
    security_opt:
      - 'apparmor:unconfined'
      - 'seccomp:unconfined'
    cap_add:
      - SYS_PTRACE
    volumes:
      - ./grafana-datasources.yaml:/etc/grafana/provisioning/datasources/datasources.yaml
      - ../dist:/var/lib/grafana/plugins/harv-logs
      - ../provisioning:/etc/grafana/provisioning
      - ..:/root/harv-logs

    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.harv-logs:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: harv-logs
      GF_INSTALL_PLUGINS: grafana-llm-app 
